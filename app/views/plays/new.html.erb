<%= simple_form_for @play do |f| %>
  <%= select_tag(:category_id, options_for_select(@categories), :prompt_text => 'Select a Category') %>
  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.input :director %>
  <%= f.submit :Button %>
<% end %>
